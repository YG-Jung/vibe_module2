# Linux Kernel 6.12 Git 로그 조회 웹 애플리케이션

Linux 커널 6.12의 git 로그를 조회하고 버전별로 분류하여 한글로 요약하는 웹 애플리케이션입니다.

## 📋 기능

- 🔍 Linux 커널 6.12 stable 브랜치의 git 로그 조회
- 📦 버전별로 커밋 그룹화 (v6.12.1, v6.12.2, ...)
- 🇰🇷 커밋 내용을 한글로 자동 요약
- 🎨 직관적이고 보기 좋은 UI
- 🔄 버전 그룹 접기/펼치기 기능
- 🔍 상세한 디버그 로그 (브라우저 콘솔)

## 🚀 실행 방법

이 애플리케이션은 **백엔드 서버**가 필요합니다. CORS 및 bot 감지 문제를 해결하기 위해 Node.js 서버를 사용합니다.

### 1단계: 백엔드 서버 실행

터미널을 열고 다음 명령어를 실행하세요:

```bash
node server.js
```

서버가 정상적으로 시작되면 다음과 같은 메시지가 표시됩니다:

```
========================================
🐧 Linux Kernel Log Server
========================================
🚀 서버 실행 중: http://localhost:3000
📡 API 엔드포인트: http://localhost:3000/api/kernel-logs
💚 헬스 체크: http://localhost:3000/health
========================================
```

### 2단계: 웹 페이지 열기

**새로운 터미널**을 열고 정적 파일 서버를 실행하세요:

#### Python 사용:
```bash
python -m http.server 8000
```

#### Node.js http-server 사용:
```bash
# 설치 (최초 1회)
npm install -g http-server

# 실행
http-server -p 8000
```

그 후 브라우저에서 `http://localhost:8000` 접속

#### 또는 직접 열기:
`index.html` 파일을 더블클릭하여 브라우저에서 직접 열 수도 있습니다.

### 3단계: 로그 조회

1. 웹 페이지를 열고 **F12**를 눌러 개발자 도구를 엽니다 (콘솔 확인용)
2. **"로그 불러오기"** 버튼을 클릭합니다
3. 로그가 자동으로 조회되고 버전별로 분류되어 표시됩니다
4. 콘솔에서 상세한 디버그 정보를 확인할 수 있습니다

## 📁 파일 구조

```
Module2/
├── index.html      # 메인 HTML 파일
├── app.js          # 프론트엔드 JavaScript
├── server.js       # 백엔드 Node.js 서버 ⭐ NEW
├── package.json    # Node.js 프로젝트 설정
├── style.css       # 스타일시트
└── README.md       # 설명 문서
```

## 🏗️ 아키텍처

```
┌─────────────┐         ┌─────────────┐         ┌──────────────────┐
│   Browser   │  HTTP   │   Node.js   │  HTTPS  │  kernel.org      │
│             │ ───────>│   Server    │ ───────>│  Git Repository  │
│ (app.js)    │  :8000  │ (server.js) │  :443   │                  │
│             │<─ ─ ─ ─ │   :3000     │<─ ─ ─ ─ │                  │
└─────────────┘   JSON  └─────────────┘   HTML  └──────────────────┘
```

### 왜 백엔드 서버가 필요한가?

1. **CORS 문제**: 브라우저의 Same-Origin Policy로 인해 kernel.org에 직접 요청할 수 없음
2. **Bot 감지**: CORS 프록시 서비스들이 bot 트래픽을 차단
3. **User-Agent**: 백엔드에서 적절한 브라우저 헤더를 설정하여 요청

## 💡 사용 방법

1. 웹 페이지를 열면 "로그 불러오기" 버튼이 표시됩니다
2. 버튼을 클릭하면 백엔드 서버를 통해 Linux 커널 6.12 로그를 가져옵니다
3. 로그는 버전별로 그룹화되어 표시됩니다
4. 각 버전 헤더를 클릭하면 해당 버전의 커밋 목록을 접거나 펼칠 수 있습니다
5. 브라우저 콘솔(F12)에서 상세한 디버그 로그를 확인할 수 있습니다

## 🔧 기술 스택

### 프론트엔드
- **HTML5**: 웹 페이지 구조
- **CSS3**: 스타일링 및 레이아웃 (Gradient, Flexbox 등)
- **JavaScript (Vanilla)**: 로그 조회 및 파싱 로직

### 백엔드
- **Node.js**: 백엔드 서버 런타임
- **HTTP/HTTPS 모듈**: 서버 구축 및 외부 요청
- **CORS 헤더**: Cross-Origin 요청 허용

## 📊 커밋 분류 기준

커밋은 다음과 같은 카테고리로 자동 분류됩니다:

- 📦 새로운 버전 릴리스
- 🔧 버그 수정
- ✨ 새로운 기능 추가
- ⬆️ 기능 개선 및 업데이트
- 🔒 보안 패치
- ⚡ 성능 최적화
- 🔌 드라이버 관련 변경
- 🌐 네트워크 서브시스템 변경
- 💾 파일시스템 변경
- 🧠 메모리 관리 변경
- ↩️ 이전 커밋 되돌림
- ♻️ 코드 리팩토링
- 📝 문서 업데이트
- 기타...

## 🐛 디버깅

### 백엔드 서버가 시작되지 않는 경우

```bash
# Node.js 버전 확인
node --version

# 포트 3000이 이미 사용 중인지 확인
# Windows:
netstat -ano | findstr :3000

# macOS/Linux:
lsof -i :3000
```

### 브라우저에서 연결 오류가 발생하는 경우

1. 백엔드 서버(`server.js`)가 실행 중인지 확인
2. 브라우저 콘솔(F12)에서 에러 메시지 확인
3. `http://localhost:3000/health`에 접속하여 서버 상태 확인

### 로그가 보이지 않는 경우

1. 브라우저 콘솔(F12)을 열어 디버그 로그 확인
2. 백엔드 서버의 터미널 출력 확인
3. 네트워크 탭에서 API 요청 상태 확인

## ⚠️ 주의사항

- Node.js가 설치되어 있어야 합니다 (v12 이상 권장)
- 백엔드 서버와 프론트엔드 서버를 모두 실행해야 합니다
- 대량의 커밋을 불러오므로 초기 로딩에 10-30초 정도 걸릴 수 있습니다
- kernel.org 서버 상태에 따라 응답 시간이 달라질 수 있습니다

## 🔗 참고 링크

- [Linux Kernel Git Repository](https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git)
- [Linux 6.12 Stable Branch](https://git.kernel.org/pub/scm/linux/kernel/git/stable/linux.git/log/?h=linux-6.12.y)
- [Node.js](https://nodejs.org/)

## 📝 API 엔드포인트

### GET /api/kernel-logs
Linux 커널 6.12 로그 HTML을 반환합니다.

**Response:**
```
Content-Type: text/html
Status: 200 OK
```

### GET /health
서버 헬스 체크

**Response:**
```json
{
  "status": "ok",
  "timestamp": "2024-01-01T00:00:00.000Z",
  "uptime": 123.456
}
```
